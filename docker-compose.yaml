version: "3.9"
services:
#  TODO: use nginx as static file server
  corg-frontend:
    image: "corg-fe:1.1"
    ports:
      - "3000:3000" # localhost-port:container-port
  corg-backend:
    image: "corg-be:local"
    ports:
      - "8080:8080"